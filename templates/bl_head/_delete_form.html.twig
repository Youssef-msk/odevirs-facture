<form method="post" action="{{ path('app_bl_head_delete', {'id': bl_head.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bl_head.id) }}">
    <button class="btn">Delete</button>
</form>
